# Gym Membership Web Dashboard

Run in PowerShell:

```powershell
python -m pip install -r requirements.txt
python app.py
```

Then open `http://127.0.0.1:5000` in Chrome.

The dashboard reads and updates `members.csv`.
